   %% 
    int   i, max, sum;

    sum = 0;
    i = 1;
    get ( max);
    while (i <  max)  {
          sum = sum + i;
          i  = i + 1; 
	}
	 
	if(sum / max == 0)
	{
		put (sum + max);
	}
	endif
